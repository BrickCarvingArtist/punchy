@import "./mixin"
.about
	display flex
	flex-direction column
	align-items center
	h1
		margin-top h(20)
		text-align center
		line-height 2
		font-size h(18)
	.row
		width w(375)
		&:last-of-type
			text-align center
	a.small
		color $blue
	.map
		margin-top h(20)
		width w(350)
		height h(350)